services:
  tftp:
    build: .
    image: tftpw
    container_name: tftpw
    restart: unless-stopped
    network_mode: host
    volumes:
      - ./data:/dboot
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
